# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  ensure:
    p99: 200
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_others.csv"
    fields:
      - "JetsonID"
    skipHeader: true

scenarios:
  - name: "Invalid parameters and authorization tests for GetStatus API"
    flow:
      - log: "Invalid JetsonID value test"
        get:
          url: "/GetStatus"
          headers:
            content-type: "application/json"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            params: 
              JetsonID: "invalidValue"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
                description: "Bad Request"
      - think: 1

      - log: "Invalid URL test"
        get:
          url: "/InvalidURL"
          headers:
            content-type: "application/json"
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            params: 
              JetsonID: "{{ JetsonID }}"
          expect:
            - statusCode: 500
            - hasProperty: "description"
            - equals: 
                description: "Parameter Error"
      - think: 1

      - log: "Invalid authorization token test"
        get:
          url: "/GetStatus"
          headers:
            content-type: "application/json"
            authorization: "invalidToken"
          qs:
            params: 
              JetsonID: "{{ JetsonID }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals: 
                description: "Bad Request"
      - think: 1
