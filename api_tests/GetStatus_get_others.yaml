# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_others.csv"
    fields:
      - "JetsonID"
    skipHeader: true

scenarios:
  - name: "Testing failure and edge cases for /GetStatus endpoint"
    flow:
      - 
        get:
          url: "/GetStatus?params="
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: headers
          beforeRequest: "log('Making request with empty params')"
      - think: 1
      - 
        get:
          url: "/GetStatus?params={%22JetsonID2%22:%20%22d290f1ee-6c54-4b01-90e6-d701748f0851%22}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: headers
          beforeRequest: "log('Making request with incorrect parameter name')"
      - think: 1
      - 
        get:
          url: "/GetStatus?params={%22JetsonID%22:%203}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: headers
          beforeRequest: "log('Making request with incorrect parameter value')"
      - think: 1
      - 
        get:
          url: "/GetStatus?params=3"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: headers
          beforeRequest: "log('Making request with incorrect params format')"
      - think: 1
      - 
        get:
          url: "/InvalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: headers
          beforeRequest: "log('Making request with invalid URL')"
      - think: 1
      - 
        get:
          url: "/GetStatus?params={%22JetsonID%22:%20%22{{ JetsonID }}%22}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: headers
          beforeRequest: "log('Making request with invalid authorization token')"
