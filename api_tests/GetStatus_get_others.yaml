# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    - think: 1
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_others.csv"
    fields: ["JetsonID"]
    skipHeader: true

scenarios:
  - flow:
    - log: "Sending a GET request with a valid JetsonID, but expecting it to fail due to invalid auth token"
      get:
        url: "/GetStatus?params={{ JetsonID }}"
        headers:
          authorization: "Invalid Auth Token"
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "message"
          - equals: ["error", "Unauthorized"]
    - think: 1

    - log: "Sending a GET request with an invalid JetsonID"
      get:
        url: "/GetStatus?params=InvalidJetsonID"
        headers:
          authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "message"
          - equals: ["error", "Bad Request"]
    - think: 1

    - log: "Sending a GET request without any JetsonID"
      get:
        url: "/GetStatus"
        headers:
          authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 500
          - hasProperty: "error"
          - hasProperty: "message"
          - equals: ["error", "Parameter Error"]
    - think: 1
