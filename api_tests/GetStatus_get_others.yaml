# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./GetStatus_get_others.csv"
    fields:
      - "JetsonID"
    skipHeader: true

scenarios:
  - name: "Testing the GetStatus API for failure cases"
    flow:
      - log: "Test Case 1: Sending a request without required query parameters"
        get:
          url: "/GetStatus?params="
          headers:
            accept: "application/json"
        expect:
          - statusCode: 400
          - hasProperty: "description"
          - equals: 
            - "description"
            - "Bad Request"
      - think: 1

      - log: "Test Case 2: Sending a request with incorrect query parameters"
        get:
          url: "/GetStatus?params={%22JetsonI%22:%20%22{{ JetsonID }}%22}"
          headers:
            accept: "application/json"
        expect:
          - statusCode: 500
          - hasProperty: "description"
          - equals: 
            - "description"
            - "Parameter Error"
      - think: 1

      - log: "Test Case 3: Sending a request with incorrect URL"
        get:
          url: "/GetStat?params={%22JetsonID%22:%20%22{{ JetsonID }}%22}"
          headers:
            accept: "application/json"
        expect:
          - statusCode: 404
      - think: 1
