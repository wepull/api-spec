# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_others.csv"
    fields:
      - "params"
    skipHeader: true

scenarios:
  - name: "API failure cases"
    flow:
      - think: 5
      - log: "Sending a request without required query parameter"
        get:
          url: "/GetStatus"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        # The request should fail as it does not include the required 'params' query parameter
      - think: 5
      - log: "Sending a request with invalid URL"
        get:
          url: "/InvalidURL"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        # The request should fail as the URL is invalid
      - think: 5
      - log: "Sending a request with invalid authorization token"
        get:
          url: "/GetStatus?params={{ params }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        # The request should fail as the authorization token is invalid
