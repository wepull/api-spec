# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Test GetStatus endpoint with error conditions"
    flow:
      - log: "Sending GetStatus request with invalid JetsonID"
        get:
          url: "/GetStatus?params={%22JetsonID%22:%20%22invalid%22}"
          headers:
            accept: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "description"
      - think: 1
      - log: "Sending GetStatus request without JetsonID"
        get:
          url: "/GetStatus?params={}"
          headers:
            accept: "application/json"
          expect:
            - statusCode: 500
            - hasProperty: "description"
      - think: 1
      - log: "Sending GetStatus request with unauthorized access"
        get:
          url: "/GetStatus?params={%22JetsonID%22:%20%22d290f1ee-6c54-4b01-90e6-d701748f0851%22}"
          headers:
            accept: "application/json"
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Sending GetStatus request to invalid URL"
        get:
          url: "/InvalidURL?params={%22JetsonID%22:%20%22d290f1ee-6c54-4b01-90e6-d701748f0851%22}"
          headers:
            accept: "application/json"
          expect:
            - statusCode: 404
