# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_others.csv"
    fields:
      - "params"
    skipHeader: true

scenarios:
  - name: "API failure cases"
    flow:
      - think: 5
      - log: "Sending a request without required query parameter"
        get:
          url: "/GetStatus"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        # The request should fail as it does not include the required 'params' parameter. The API should return a 400 status code and an error message.
      - think: 5
      - log: "Sending a request with an invalid URL"
        get:
          url: "/GetStatu"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        # The request should fail as the URL is invalid. The API should return a 404 status code.
      - think: 5
      - log: "Sending a request with invalid authorization token"
        get:
          url: "/GetStatus?params={{ params }}"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        # The request should fail as the authorization token is invalid. The API should return a 401 status code and an error message.
      - think: 5
      - log: "Sending a request with a non-existing JetsonID"
        get:
          url: "/GetStatus?params={\"JetsonID\":\"nonExistingID\"}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "error"
        # The request should fail as the JetsonID does not exist. The API should return a 500 status code and an error message.
