# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_others.csv"
    fields:
      - "JetsonID"
    skipHeader: true

scenarios:
  - name: "Testing failure and edge cases for /GetStatus endpoint"
    flow:
      - 
        get:
          url: "/GetStatus?params="
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: headers
          beforeRequest:
            log: "Sending request with empty params. Expecting 400 response"
      - think: 1
      -
        get:
          url: "/GetStatus?params={%22JetsonID2%22:%20%22d290f1ee-6c54-4b01-90e6-d701748f0851%22}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: headers
          beforeRequest:
            log: "Sending request with incorrect param key. Expecting 500 response"
      - think: 1
      -
        get:
          url: "/GetStatus?params={%22JetsonID%22:%203}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: headers
          beforeRequest:
            log: "Sending request with JetsonID as a number instead of string. Expecting 400 response"
      - think: 1
      -
        get:
          url: "/GetStatus?params=3"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: headers
          beforeRequest:
            log: "Sending request with params as a number instead of object. Expecting 400 response"
      - think: 1
      -
        get:
          url: "/invalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: headers
          beforeRequest:
            log: "Sending request to invalid URL. Expecting 404 response"
      - think: 1
      -
        get:
          url: "/GetStatus?params={%22JetsonID%22:%20%22d290f1ee-6c54-4b01-90e6-d701748f0851%22}"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: headers
          beforeRequest:
            log: "Sending request with invalid authorization token. Expecting 401 response"
      - think: 1
      -
        get:
          url: "/GetStatus?params={%22JetsonID%22:%20%22{{ JetsonID }}%22}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: headers
          beforeRequest:
            log: "Sending request with JetsonID from CSV file. Expecting 500 response if JetsonID does not exist"
