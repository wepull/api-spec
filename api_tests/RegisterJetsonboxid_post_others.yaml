# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c528c38714

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "RegisterJetsonboxid_post_others.csv"
    fields:
      - "jetson_uuid"
      - "username"
      - "clusterId"
      - "houseId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Testing /RegisterJetsonboxid API"
    flow:
      - log: "Starting test with Invalid URL"
      - get:
          url: "/InvalidURL"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - log: "Invalid URL test complete"
      - think: 1
      - log: "Starting test with Invalid Authorization"
      - post:
          url: "/RegisterJetsonboxid"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
            username: "{{ username }}"
            clusterId: "{{ clusterId }}"
            houseId: "{{ houseId }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - log: "Invalid Authorization test complete"
      - think: 1
      - log: "Starting test with Invalid Parameters"
      - post:
          url: "/RegisterJetsonboxid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid: "Invalid"
            username: "{{ username }}"
            clusterId: "{{ clusterId }}"
            houseId: "{{ houseId }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - log: "Invalid Parameters test complete"
      - think: 1
      - log: "Starting test with Missing Parameters"
      - post:
          url: "/RegisterJetsonboxid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            clusterId: "{{ clusterId }}"
            houseId: "{{ houseId }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - log: "Missing Parameters test complete"
