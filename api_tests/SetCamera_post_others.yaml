# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=06e98559a3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "SetCamera_post_others.csv"
    fields:
      - "params"
    skipHeader: true
scenarios:
  - name: "Testing /SetCamera API endpoint"
    flow:
      - think: 5
      - log: "Test case: Invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            params: "{{ params }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 5
      - log: "Test case: Missing required parameter"
        post:
          url: "/SetCamera"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            params: ""
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 5
      - log: "Test case: Invalid Authorization token"
        post:
          url: "/SetCamera"
          headers:
            Authorization: "InvalidToken"
          json:
            params: "{{ params }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 5
      - log: "Test case: Server error"
        post:
          url: "/SetCamera"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            params: "InvalidData"
          expect:
            - statusCode: 500
            - hasProperty: "message"
