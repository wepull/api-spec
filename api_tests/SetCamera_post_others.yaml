# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=06e98559a3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "SetCamera_post_others.csv"
    fields:
      - "params"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "API robustness test"
    flow:
      - log: "Invalid payload test"
        post:
          url: "/SetCamera"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            params: "invalid_param"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals:
                - "{{ response.body.description }}"
                - "Bad Request"
        think: 5
      - log: "Invalid URL test"
        post:
          url: "/InvalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            params: "{{ params }}"
          expect:
            - statusCode: 404
        think: 5
      - log: "Invalid auth test"
        post:
          url: "/SetCamera"
          headers:
            Authorization: "invalid_token"
          json:
            params: "{{ params }}"
          expect:
            - statusCode: 401
        think: 5
      - log: "No payload test"
        post:
          url: "/SetCamera"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "description"
            - equals:
                - "{{ response.body.description }}"
                - "Parameter Error"
