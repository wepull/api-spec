# ********RoostGPT********

# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /api/device/camera/alert_post for http method type POST in artillery framework
# RoostTestHash=ba1d0a9484
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "api_device_camera_alert_post_success.csv"
    fields:
      - "jetson_uuid"
      - "camera_id"
      - "status"
      - "timestamp"
    skipHeader: true
    delimiter: "^|^"
  headers:
    Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Camera Alert API Test"
    flow:
      - log: "POST /api/device/camera/alert"
      - post:
          url: "/api/device/camera/alert"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
            camera_id: "{{ camera_id }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 201
            - hasProperty: "jetson_uuid"
            - hasProperty: "camera_id"
            - hasProperty: "status"
            - hasProperty: "timestamp"
      - think: 1
