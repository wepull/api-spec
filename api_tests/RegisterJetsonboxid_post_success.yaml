# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /RegisterJetsonboxid_post for http method type POST in artillery framework
# RoostTestHash=c528c38714

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "RegisterJetsonboxid_post_success.csv"
    fields: ["jetson_uuid", "username", "clusterId", "houseId"]
    skipHeader: true

scenarios:
  - name: "Register Houseid and Password to DMServer"
    flow:
      - get:
          url: "/RegisterJetsonboxid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            params:
              jetson_uuid: "{{ jetson_uuid }}"
              username: "{{ username }}"
              clusterId: "{{ clusterId }}"
              houseId: "{{ houseId }}"
          expect:
            - statusCode: 200
            - hasProperty: 'params'
            - hasHeader: 'content-type'
          log: "Registered Houseid and Password to DMServer successfully"
          think: 1
