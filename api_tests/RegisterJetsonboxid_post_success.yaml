# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /RegisterJetsonboxid_post for http method type POST in artillery framework
# RoostTestHash=c528c38714

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "RegisterJetsonboxid_post_success.csv"
    fields:
      - "jetson_uuid"
      - "username"
      - "clusterId"
      - "houseId"
    skipHeader: true

scenarios:
  - name: "Register Houseid and Password to DMServer"
    flow:
      - log: "Starting test scenario for /RegisterJetsonboxid API"
      - get:
          url: "/RegisterJetsonboxid?clusterId={{ clusterId }}&houseId={{ houseId }}&jetson_uuid={{ jetson_uuid }}&username={{ username }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'description'
            - equals: 
                description: 'Result OK'
          capture:
            json: "$.description"
            as: "response"
      - log: "The response from the POST request is {{ response }}"
      - think: 1
