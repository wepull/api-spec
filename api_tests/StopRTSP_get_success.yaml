# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /StopRTSP_get for http method type GET in artillery framework
# RoostTestHash=6d23a25a06

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "StopRTSP_get_success.csv"
    fields: ["JetsonID", "camera_id"]
    skipHeader: true

scenarios:
  - name: "Stop RTSP link of each camera"
    flow:
      - log: "Test for stopRTSP API"
      - get:
          url: "/StopRTSP?params={{JetsonID}}&camera_id={{camera_id}}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
          think: 1
      - log: "Request to stopRTSP API was successful"
