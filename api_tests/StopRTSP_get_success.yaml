# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /StopRTSP_get for http method type GET in artillery framework
# RoostTestHash=6d23a25a06

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "StopRTSP_get_success.csv"
    fields:
      - "jetson_id"
      - "camera_id"
    skipHeader: true

scenarios:
  - name: "Stop the rtsp link of each camera"
    flow:
      - get:
          url: "/StopRTSP?JetsonID={{ jetson_id }}&camera_id={{ camera_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "JetsonID"
            - hasProperty: "camera_id"
          log: "Stopping the rtsp link of each camera with JetsonID: {{ jetson_id }} and camera_id: {{ camera_id }}"
        think: 1
