# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=6d23a25a06

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p: 99
    rps: 50
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "StopRTSP_get_others.csv"
    fields:
      - "jetson_id"
      - "camera_id"
    skipHeader: true

scenarios:
  - name: "Invalid and edge case scenarios for StopRTSP API"
    flow:
      - log: "Sending a request with invalid query parameters"
        get:
          url: "/StopRTSP?params={{ params }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals:
                description: "Bad Request"
      - think: 5

      - log: "Sending a request with missing query parameters"
        get:
          url: "/StopRTSP"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals:
                description: "Bad Request"
      - think: 5

      - log: "Sending a request with invalid URL"
        get:
          url: "/InvalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - equals:
                description: "Bad Request"
      - think: 5

      - log: "Sending a request with invalid Authorization token"
        get:
          url: "/StopRTSP?params={{ params }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "description"
            - equals:
                description: "Parameter Error"
      - think: 5
