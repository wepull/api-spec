# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3f25b96b7c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "GetRTSPURL_get_others.csv"
    fields:
      - "cameraId"
      - "jetson_uuid"
    skipHeader: true

scenarios:
  - name: "Testing API endpoint /GetRTSPURL for failure cases, edge cases, and error handling"
    flow:
      - log: "Sending a request with missing query parameters"
        get:
          url: "/GetRTSPURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
        think: 1

      - log: "Sending a request with invalid URL"
        get:
          url: "/InvalidURL?cameraId={{ cameraId }}&jetson_uuid={{ jetson_uuid }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1

      - log: "Sending a request with invalid authorization token"
        get:
          url: "/GetRTSPURL?cameraId={{ cameraId }}&jetson_uuid={{ jetson_uuid }}"
          headers:
            Authorization: "InvalidToken"
        expect:
          - statusCode: 401
          - hasProperty: "error"
        think: 1

      - log: "Sending a request with invalid query parameters"
        get:
          url: "/GetRTSPURL?cameraId=Invalid&jetson_uuid=Invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 500
          - hasProperty: "error"
        think: 1
