# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3f25b96b7c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    maxErrorRate: 1
  plugins:
    expect: {}
  payload:
    path: "GetRTSPURL_get_others.csv"
    fields: ["jetson_uuid", "cameraId"]
    skipHeader: true

scenarios:
  - name: "API endpoint tests"
    flow:
      - think: 1
      - log: "Invalid payload test"
      # Invalid payload test
      - post:
          url: "/GetRTSPURL?params={{invalid_payload}}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1
      - log: "Invalid URL test"
      # Invalid URL test
      - post:
          url: "/InvalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Invalid authorization test"
      # Invalid authorization test
      - post:
          url: "/GetRTSPURL?params={{jetson_uuid, cameraId}}"
          headers:
            Authorization: "{{invalid_auth}}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1
      - log: "Server error test"
      # Server error test
      - post:
          url: "/GetRTSPURL?params={{jetson_uuid, cameraId}}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
            - hasProperty: "error"
