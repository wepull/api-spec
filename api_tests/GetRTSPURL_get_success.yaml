# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /GetRTSPURL_get for http method type GET in artillery framework
# RoostTestHash=3f25b96b7c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  ensure:
    p95: 200
  plugins:
    expect: {}
  payload:
    path: "GetRTSPURL_get_success.csv"
    fields:
      - "cameraId"
      - "jetson_uuid"
    skipHeader: true

scenarios:
  - name: "Get the rtsp link of each camera"
    flow:
      - get:
          url: "/GetRTSPURL?cameraId={{ cameraId }}&jetson_uuid={{ jetson_uuid }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          beforeRequest:
            - "log": "Sending GET request with cameraId: {{ cameraId }} and jetson_uuid: {{ jetson_uuid }}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
          think: 1
