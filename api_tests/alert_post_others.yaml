# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# RoostTestHash=e5fc604c97

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "alert_post_others.csv"
    fields: ["cameraId", "status", "timestamp"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Failure and Edge Test Cases for cameraAlert API"
    flow:
      - log: "Test Case: cameraAlert API with invalid url"
        post:
          url: "/api/device/camera/invalid_url"
          json:
            cameraId: "{{ cameraId }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
        think: 5

      - log: "Test Case: cameraAlert API with invalid authorization token"
        post:
          url: "/api/device/camera/alert"
          json:
            cameraId: "{{ cameraId }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
        think: 5

      - log: "Test Case: cameraAlert API with missing mandatory field in the request body"
        post:
          url: "/api/device/camera/alert"
          json:
            cameraId: "{{ cameraId }}"
            timestamp: "{{ timestamp }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
        think: 5

      - log: "Test Case: cameraAlert API with invalid data type in the request body"
        post:
          url: "/api/device/camera/alert"
          json:
            cameraId: 123
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
