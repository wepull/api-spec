# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# RoostTestHash=ba1d0a9484

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "alert_post_others.csv"
    fields: ["jetson_uuid", "camera_id", "status", "timestamp"]
    skipHeader: true
  environments:
    development:
      target: "http://localhost:3000"
      phases:
        - duration: 60
          arrivalRate: 5
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Negative test case for API - /api/device/camera/alert"
    flow:
      - log: "Sending invalid payload - missing required fields"
        post:
          url: "/api/device/camera/alert"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
            camera_id: "{{ camera_id }}"
            status: "{{ status }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals:
                - "{{ response.body.error }}": "Invalid input, object invalid"
      - think: 1
      - log: "Sending request with invalid Authorization token"
        post:
          url: "/api/device/camera/alert"
          headers:
            Authorization: "InvalidToken"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
            camera_id: "{{ camera_id }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals:
                - "{{ response.body.error }}": "Unauthorized"
      - think: 1
      - log: "Sending request to invalid URL"
        post:
          url: "/api/device/camera/alert_invalid"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
            camera_id: "{{ camera_id }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - equals:
                - "{{ response.body.error }}": "URL not found"
