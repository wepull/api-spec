# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /SetCamera_post for http method type POST in artillery framework
# RoostTestHash=06e98559a3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./SetCamera_post_success.csv"
    fields:
      - "jetson_id"
    skipHeader: true
  environments:
    development:
      target: "http://localhost:3000"
      http:
        pool: 10

scenarios:
  - name: "Set parameter to the specific camera based on applied feature"
    flow:
      - get:
          url: "/SetCamera"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            params:
              jetson_id: "{{ jetson_id }}"
          expect:
            - statusCode: 200
            - hasProperty: "params"
          log: "Request to set camera parameters successful."
        think: 1
