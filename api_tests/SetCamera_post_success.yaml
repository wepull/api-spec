# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /SetCamera_post for http method type POST in artillery framework
# RoostTestHash=06e98559a3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my_processor.js"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "SetCamera_post_success.csv"
    fields: ["jetson_id"]
    skipHeader: true
  environments:
    dev:
      target: "http://0.0.0.0:4010"
      phases:
        - duration: 10
          arrivalRate: 1

scenarios:
  - name: "Set Camera parameters"
    flow:
      - get:
          url: "/SetCamera?params={{ jetson_id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "OK"
          log: "Setting Camera parameters with jetson_id: {{ jetson_id }}"
        think: 1
