# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# Test generated for /GetStatus_get for http method type GET in artillery framework

# RoostTestHash=bd533aa383


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetStatus_get_success.csv"
    fields:
      - "JetsonID"
    skipHeader: true

scenarios:
  - name: "Get the status of device Jetson Nano including the Cameras"
    flow:
      - log: "Performing a GetStatus request with valid JetsonID"
      - get:
          url: "/GetStatus?params={{ JetsonID }}"
          headers:
            accept: "application/json"
            user-agent: "PostmanRuntime/7.35.0"
            postman-token: "4ef94f7d-2d18-45b2-9343-a73685a8b249"
            accept-encoding: "gzip, deflate, br"
            connection: "keep-alive"
          expect:
            - statusCode: 200
      - think: 1
