# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /GetStatus_get for http method type GET in artillery framework
# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetStatus_get_success.csv"
    fields:
      - "JetsonID"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Get the status of device Jetson Nano including the Cameras"
    flow:
      - log: "Sending a GET request to /GetStatus with valid JetsonID"
      - get:
          url: "/GetStatus?params={{ JetsonID }}"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "content"
            - hasProperty: "content.application/json"
            - hasProperty: "content.application/json.schema"
            - hasProperty: "content.application/json.schema.$ref"
        # After successfully getting the device status, the server should return a 200 status code with the device status in the response body.
      - think: 1
