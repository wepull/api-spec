# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /GetStatus_get for http method type GET in artillery framework
# RoostTestHash=bd533aa383

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p99: 300
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "GetStatus_get_success.csv"
    fields:
      - "JetsonID"
    skipHeader: true

scenarios:
  - name: "Get the status of device Jetson Nano including the Cameras"
    flow:
      - log: "Get the status of device Jetson Nano including the Cameras"
      - get:
          url: "/GetStatus"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            params:
              JetsonID: "{{ JetsonID }}"
          expect:
            - statusCode: 200
            - header: "Content-Type"
            - hasProperty: "result"
            - hasProperty: "JetsonID"
          think: 5
