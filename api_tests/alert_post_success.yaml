# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4

# Test generated for /api/device/alert_post for http method type POST in artillery framework
# RoostTestHash=d0cccd6f73

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "alert_post_success.csv"
    fields:
      - "device_id"
      - "status"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Device Alert API test"
    flow:
      - post:
          url: "/api/device/alert"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            device_id: "{{ device_id }}"
            status: "{{ status }}"
          beforeRequest: "log"
          expect:
            - statusCode: 201
            - hasProperty: "message"
            - hasProperty: "data.device_id"
            - hasProperty: "data.status"
          think: 1
