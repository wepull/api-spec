# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# Test generated for /api/device/camera/alert_post for http method type POST in artillery framework

# RoostTestHash=ba1d0a9484


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "alert_post_success.csv"
    fields: ["jetson_uuid", "camera_id", "status", "timestamp"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Send camera online/offline status to user via etown"
    flow:
      - post:
          url: "/api/device/camera/alert"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
            camera_id: "{{ camera_id }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          log: "Posting a new alert for camera status"
          think: 1
          expect:
            - statusCode: 201
              hasProperty: ['jetson_uuid', 'camera_id', 'status', 'timestamp']
            - equals:
                - "{{ $responseHeaders.Content-Type }}"
                - "application/json"
