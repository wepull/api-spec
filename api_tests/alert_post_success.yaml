# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# Test generated for /api/device/camera/alert_post for http method type POST in artillery framework

# RoostTestHash=ba1d0a9484


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "alert_post_success.csv"
    fields:
      - "jetson_uuid"
      - "camera_id"
      - "status"
      - "timestamp"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 1
scenarios:
  - name: "Camera Alert API Scenario"
    flow:
      - log: "Sending POST request to /api/device/camera/alert"
      - post:
          url: "/api/device/camera/alert"
          headers:
            content-type: "application/json"
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
            camera_id: "{{ camera_id }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          expect:
            - statusCode: 201
            - hasProperty: "jetson_uuid"
            - hasProperty: "camera_id"
            - hasProperty: "status"
            - hasProperty: "timestamp"
      - think: 1
