# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# Test generated for /api/device/camera/alert_post for http method type POST in artillery framework

# RoostTestHash=e5fc604c97


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "alert_post_success.csv"
    fields:
      - "cameraId"
      - "status"
      - "timestamp"
    skipHeader: true
  processor: "./processor.js"

scenarios:
  - name: "Camera Alert Test Scenario"
    flow:
      - post:
          url: "/api/device/camera/alert"
          json:
            cameraId: "{{ cameraId }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.response"
            as: "response"
          expect:
            - statusCode: 201
            - hasProperty: "response"
          log: "Post Request to create camera alert was successful."

      - think: 1
