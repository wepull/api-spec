# Test generated by RoostGPT for test IPFront-Test using AI Type Open AI and AI Model gpt-4


# Test generated for /api/device/camera/alert_post for http method type POST in artillery framework

# RoostTestHash=e5fc604c97


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "alert_post_success.csv"
    fields:
      - "jetson_uuid"
      - "camera_id"
      - "status"
      - "timestamp"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Camera Alert Test Scenario"
    flow:
      - post:
          url: "/api/device/camera/alert"
          headers:
            content-type: "application/json"
          json:
            jetson_uuid: "{{ jetson_uuid }}"
            camera_id: "{{ camera_id }}"
            status: "{{ status }}"
            timestamp: "{{ timestamp }}"
          expect:
            - statusCode: 201
            - hasProperty: "headers"
          log: "Valid request to send camera alert"
          think: 1
